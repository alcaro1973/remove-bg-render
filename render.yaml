services:
  - type: web
    name: bg-remove-api
    env: python
    buildCommand: |
      pip install -r requirements.txt
      npm install
    startCommand: node index.js
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
